# ---------------------------------------------------------------------------
# Cloud+X PBL & Hackathon Archives - root .gitignore
#
# 각 프로젝트 디렉터리(2026/1 ~ 2026/5)는 자체 .gitignore를 가지고 있습니다.
# 이 파일은 저장소 전체에 공통으로 적용되는 규칙만 정의합니다.
# 프로젝트별 빌드 산출물 규칙은 해당 프로젝트의 .gitignore에서 관리하세요.
# ---------------------------------------------------------------------------

# --- OS ---------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# --- 에디터 / IDE -----------------------------------------------------------
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo
*~
.fleet/
*.sublime-workspace

# --- 비밀정보 / 로컬 환경설정 ------------------------------------------------
.env
.env.local
.env.*.local
!.env.example
!.env.sample
*.pem
*.p12
*.keystore
secrets.json
credentials.json
.mnemonic
.secret

# --- 로그 / 임시 파일 --------------------------------------------------------
*.log
*.tmp
*.bak
*.orig
*.rej
tmp/
temp/

# --- 언어별 공통 캐시 (프로젝트 .gitignore 누락 대비) ------------------------
# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Node / TypeScript
node_modules/
*.tsbuildinfo
.eslintcache

# Go
*.test
go.work
go.work.sum

# --- Solidity 빌드 산출물 (forge build / hardhat compile 로 재생성 가능) ------
# 배포 기록(broadcast/)은 컨트랙트 주소가 남으므로 아카이브 가치가 있어 추적합니다.
out/
cache/
cache_hardhat/
artifacts/
typechain/
typechain-types/
**/broadcast/**/dry-run/
**/broadcast/*/31337/

# --- 대용량 산출물 (아카이브 저장소이므로 소스/문서 위주로 유지) --------------
*.ckpt
*.pt
*.pth
*.safetensors
*.onnx
wandb/
mlruns/

# --- 에이전트 / 도구 로컬 설정 -----------------------------------------------
.claude/settings.local.json
CLAUDE.local.md
